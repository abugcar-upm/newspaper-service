import { Ng2SearchPipePipe } from './ng2-search-pipe.pipe';

describe('Ng2SearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new Ng2SearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
